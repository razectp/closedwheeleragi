# ClosedWheelerAGI Environment Configuration
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!

# ===========================================
# API Configuration (REQUIRED)
# ===========================================

# Generic API Key (use this for any provider)
API_KEY=your-api-key-here

# Or use provider-specific keys (alternative to API_KEY)
# OPENAI_API_KEY=sk-...
# NVIDIA_API_KEY=nvapi-...
# ANTHROPIC_API_KEY=sk-ant-...

# API Base URL (optional, defaults in config.json)
# API_BASE_URL=https://api.openai.com/v1
# API_BASE_URL=https://integrate.api.nvidia.com/v1
# API_BASE_URL=https://api.anthropic.com/v1

# Model to use (optional, defaults in config.json)
# MODEL=gpt-4o-mini
# MODEL=minimaxai/minimax-m2.1
# MODEL=claude-sonnet-4-5-20250929

# Provider override (optional, auto-detected from model/key if omitted)
# PROVIDER=openai
# PROVIDER=anthropic

# ===========================================
# Fallback Models (OPTIONAL)
# ===========================================

# Configure fallback models in config.json, not here
# These will automatically be used if primary model is slow/fails
# Example in config.json:
# "fallback_models": ["gpt-4o-mini", "gpt-3.5-turbo"]
# "fallback_timeout": 30

# ===========================================
# Telegram Integration (OPTIONAL)
# ===========================================

# Telegram Bot Token (get from @BotFather)
# TELEGRAM_BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# Telegram Chat ID (your user ID or group ID)
# Can be set here OR in config.json under telegram.chat_id
# TELEGRAM_CHAT_ID=123456789

# ===========================================
# Advanced Settings (OPTIONAL)
# ===========================================

# Override settings from config.json
# OPENAI_BASE_URL=https://custom-endpoint.com/v1
# OPENAI_MODEL=custom-model-name

# ===========================================
# Security Best Practices
# ===========================================
# 1. Never commit .env files to git (already in .gitignore)
# 2. Use different API keys for development and production
# 3. Rotate API keys regularly (every 90 days recommended)
# 4. Use read-only or scoped API keys when possible
# 5. Keep API keys in secure password managers

# ===========================================
# Quick Setup Examples
# ===========================================

# Example 1: OpenAI
# API_KEY=sk-proj-1234567890abcdefghijklmnop
# API_BASE_URL=https://api.openai.com/v1
# MODEL=gpt-4o-mini

# Example 2: NVIDIA NIM
# API_KEY=nvapi-xxxxxxxxxxxxxxxxxxx
# API_BASE_URL=https://integrate.api.nvidia.com/v1
# MODEL=minimaxai/minimax-m2.1

# Example 3: Anthropic Claude (native support, no proxy needed)
# API_KEY=sk-ant-api03-1234567890
# API_BASE_URL=https://api.anthropic.com/v1
# MODEL=claude-sonnet-4-5-20250929
# PROVIDER=anthropic
#
# Note: Provider is auto-detected for claude-* models and sk-ant-* keys.
# Setup tokens (sk-ant-oat01-*) use Bearer auth automatically.
